{
  "name": "@disk-tree/react",
  "version": "0.1.0",
  "description": "disk-tree's React widgets (StalenessScatter, AgeHistograms, TimeSeries) — bytes/mtime/age-domain views built on the @rdub/treemap core, which it also re-exports. Accessor-based, chart-lib-free.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@rdub/treemap": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "jsdom": "^25.0.1",
    "typescript": "~5.9.3",
    "vitest": "^2.1.9"
  }
}
