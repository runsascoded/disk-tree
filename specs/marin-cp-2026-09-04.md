# CP manifest: marin → upstream, 2026-09-04

Written from the marin-gcs-usage `cw-s3` session after a `/cp dt/main` pull
(upstream `93e24b0..25f3dc0^` react + `588c4bf` landed there; see marin's
`specs/branch-parity-discipline.md` scramble log). What marin holds that
upstream lacks, so upstream stays a superset:

## `packages/react` — `TimeSeries.tsx` (now `@disk-tree/react`'s disk-widget half)

Apply from `cw-s3` (identical on `gcs`); all additive, default-off:

- `yFrom?: 'zero' | 'data'` — fit the linear y-range to the series (5% pad)
  instead of anchoring at zero; `[series, getX, getY, yScale, yFrom]` deps.
  Origin: gcs `974ee7c`.
- `annotations?: Annotation[]` (`{x, y, label, below?}`) — haloed text
  callouts beside points, anchor leaning away from the nearest side edge
  (`start`/`middle`/`end` by 15% bands); CSS vars `--dt-ts-anno-ink`,
  `--dt-ts-anno-halo`. Exported type `Annotation` from `index.ts`.
  Origin: cw-s3 `00dd02b`.
- `onPickX?: (x: number) => void` — click on the snapped crosshair x; sets
  `cursor: pointer` on the plot. Origin: cw-s3 `f3c5a03`.

`git diff dt/main cw-s3 -- packages/react/src/TimeSeries.tsx` is exactly
these (plus the `./colors` vs `@rdub/treemap` import line, which is yours).
Tests: `packages/react/tests/TimeSeries.test.tsx` on marin has none for
these three; worth adding upstream.

## `src/disk_tree/find/bulk_s3.py` — `S3BulkLister` adaptive retries

cw-s3 `e018da3` (on gcs too): `botocore.config.Config(retries={"mode":
"adaptive", "max_attempts": DT_S3_MAX_ATTEMPTS (default 10)})` merged with
the existing `DT_S3_ADDRESSING_STYLE` config. Motivation in the commit body:
CoreWeave CAIOS `SlowDown` bursts under 64 concurrent list streams killed
the bulk-list with botocore's default 5 attempts / no client-side rate
limiting; `adaptive` gives each worker thread its own token bucket.
`tests/test_bulk_s3.py` on cw-s3 has the config assertions.

## Still open from 2026-08-28

`specs/marin-python-cp-2026-08-28.md` (`tree_build.py` additive-field model
+ floors, access-plane productionization, `dt access sizes`) has not landed
upstream — `git diff dt/main cw-s3 -- src/disk_tree/tree_build.py
src/disk_tree/access` is unchanged since that manifest.

## Not requested

The `@rdub/treemap` extraction (`25f3dc0`) is *not* being mirrored on marin
yet — marin applied the pre-extraction content diff and keeps
`packages/react` as one vendored package until the re-pin lands on both
marin branches together. Nothing for upstream to do; noted so the next
didi run against `packages/react/src` reads as the file move, not drift.

When landed, note upstream SHAs in marin's `specs/dt-core-upstreaming.md`
sync state (gcs) / `specs/branch-parity-discipline.md` (both).
