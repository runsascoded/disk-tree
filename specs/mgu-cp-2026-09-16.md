# `/cp` pass — gcs + cw-s3 → disk-tree, 2026-09-16

Survey of the two mgu deployment peers (`m/gcs` @ `6d8a782`, `m/cw-s3`) against dt
`HEAD` (`5432a5e`), in service of the standing goal: **upstream as much of what
gcs + cw-s3 collectively built as possible, so dt users can reuse the pieces**
(scan/index infra + reusable IaC recipes, webapp, Slack/Discord comms).

## Outcome: nothing to cherry-pick

The **shared surfaces are already at parity or dt-ahead** — a mechanical CP lands
nothing:

| surface | dt vs `m/gcs` | reading |
|---|---|---|
| `packages/treemap`, `packages/react` | 147 ins / 1009 del | peers **strip** dt tests (`exportImage.test.ts`, `colors`, `cssColor`) — dt ahead. Ledger: "parity, both directions (8/28)". |
| `src/disk_tree` | 433 ins / 4900 del | peers **strip** dt's local/desktop features (`extents`, `reclaim`, `overcount`, `repos`, `snapshots`, `diff_index`, `sidecar`, `library`, half of `server.py`/`config.py`). dt is the **superset upstream** (ledger; existing manifest `done/marin-python-cp-2026-08-28.md`). |

`m/gcs` and `m/cw-s3` are byte-identical across these surfaces (they're being
aligned), so both peers give the same answer. The established upstream channel is
the dated manifests in `specs/done/` (last: `marin-cp-2026-09-07.md`, landed);
nothing has accrued past it in the shared code.

## The real upstreaming target: mgu's cloud stack (net-new to dt)

These mgu top-level surfaces have **no dt counterpart** and hold the infra the
goal is about. They're deployment-shaped, not core-shaped — upstreaming is a
**design + port (spec) effort, not a cherry-pick** (ledger already rules the www
arch "intrinsically different: `site/` Vite+CFN vs `ui/` Vite+Flask"):

| mgu surface | files | what's in it | dt today |
|---|---|---|---|
| `cloud/src/dt_cloud/` | 53 | mark & sweep engine (`sweep_plan/exec`, `mark`, `rules`, `signals`, `reactive`), digests (`digest`, `digest_plot`), **comms** (`discord_api`), identity (`identity`, `usernames`), `gcp`, `healthcheck`, `access` | none (dt has only `access`) |
| `job/` + `Dockerfile` + `cloudbuild.yaml` | ~52 | containerized scan/index job + GCP build/deploy recipe (**IaC**) | `capture`/`reduce` CLIs + `.github/workflows/reduce.yml` (parallel, GHA-based) |
| `site/functions/_lib/` | ~20 | serverless read/mark BE (`auth`, `edgeCache`, `ledger`, `marks`, `owners`, `scope`, `totals`, `resolve`) | parallel `ui/cfn` read layer (footer sidecar, tier reads) |
| `sheet-sync/` | 6 | Google-Sheets sync | none |

## Proposed roadmap (spec-driven, prioritized by reuse × readiness)

1. **Comms** (`discord_api` + digest → a generic `disk_tree` notify/digest module).
   Highest reuse, least entanglement with the mark axis; net-new to dt. Slack
   sibling to be added.
2. **IaC recipes**: reconcile mgu's container `job/` + `cloudbuild.yaml` with dt's
   `capture`/`reduce` + `reduce.yml` + `specs/iac.md` into shared, documented
   recipes (container **and** GHA).
3. **Mark & sweep** (`dt_cloud` sweep/mark/rules/reactive) → a reusable engine.
   **Blocked on gcs↔cw-s3 convergence of the mark axis** — upstream the unified
   design, not one deployment's.
4. **Serverless BE** (`site/functions/_lib`) ↔ dt's `ui/cfn`: reconcile the
   overlapping read/planning layer; the mark/owner/scope API is net-new.

## Cursor

Assessed `m/gcs` @ `6d8a782` and `m/cw-s3` @ tip: shared surfaces at parity/
dt-ahead (no CP); cloud-stack surfaces deferred to the roadmap above, gated on
gcs↔cw-s3 alignment. Next pass starts from these tips.
