# `/cp` pass — gcs + cw-s3 → disk-tree, 2026-09-16

Survey of the two mgu deployment peers (`m/gcs` @ `6d8a782`, `m/cw-s3`) against dt
`HEAD` (`5432a5e`), in service of the standing goal: **upstream as much of what
gcs + cw-s3 collectively built as possible, so dt users can reuse the pieces**
(scan/index infra + reusable IaC recipes, webapp, Slack/Discord comms).

## Outcome: nothing to cherry-pick

The **shared surfaces are already at parity or dt-ahead** — a mechanical CP lands
nothing:

| surface | dt vs `m/gcs` | reading |
|---|---|---|
| `packages/treemap`, `packages/react` | 147 ins / 1009 del | peers **strip** dt tests (`exportImage.test.ts`, `colors`, `cssColor`) — dt ahead. Ledger: "parity, both directions (8/28)". |
| `src/disk_tree` | 433 ins / 4900 del | peers **strip** dt's local/desktop features (`extents`, `reclaim`, `overcount`, `repos`, `snapshots`, `diff_index`, `sidecar`, `library`, half of `server.py`/`config.py`). dt is the **superset upstream** (ledger; existing manifest `done/marin-python-cp-2026-08-28.md`). |

`m/gcs` and `m/cw-s3` are byte-identical across these surfaces (they're being
aligned), so both peers give the same answer. The established upstream channel is
the dated manifests in `specs/done/` (last: `marin-cp-2026-09-07.md`, landed);
nothing has accrued past it in the shared code.

## The real upstreaming target: mgu's cloud stack (net-new to dt)

These mgu top-level surfaces have **no dt counterpart** and hold the infra the
goal is about. They're deployment-shaped, not core-shaped — upstreaming is a
**design + port (spec) effort, not a cherry-pick** (ledger already rules the www
arch "intrinsically different: `site/` Vite+CFN vs `ui/` Vite+Flask"):

| mgu surface | files | what's in it | dt today |
|---|---|---|---|
| `cloud/src/dt_cloud/` | 53 | mark & sweep engine (`sweep_plan/exec`, `mark`, `rules`, `signals`, `reactive`), digests (`digest`, `digest_plot`), **comms** (`discord_api`), identity (`identity`, `usernames`), `gcp`, `healthcheck`, `access` | none (dt has only `access`) |
| `job/` + `Dockerfile` + `cloudbuild.yaml` | ~52 | containerized scan/index job + GCP build/deploy recipe (**IaC**) | `capture`/`reduce` CLIs + `.github/workflows/reduce.yml` (parallel, GHA-based) |
| `site/functions/_lib/` | ~20 | serverless read/mark BE (`auth`, `edgeCache`, `ledger`, `marks`, `owners`, `scope`, `totals`, `resolve`) | parallel `ui/cfn` read layer (footer sidecar, tier reads) |
| `sheet-sync/` | 6 | Google-Sheets sync | none |

## Proposed roadmap (spec-driven, prioritized by reuse × readiness)

1. **Comms** (`discord_api` + digest → a generic `disk_tree` notify/digest module).
   Highest reuse, least entanglement with the mark axis; net-new to dt. Slack
   sibling to be added.
2. **IaC recipes**: reconcile mgu's container `job/` + `cloudbuild.yaml` with dt's
   `capture`/`reduce` + `reduce.yml` + `specs/iac.md` into shared, documented
   recipes (container **and** GHA).
3. **Mark & sweep** (`dt_cloud` sweep/mark/rules/reactive) → a reusable engine.
   **Blocked on gcs↔cw-s3 convergence of the mark axis** — upstream the unified
   design, not one deployment's.
4. **Serverless BE** (`site/functions/_lib`) ↔ dt's `ui/cfn`: reconcile the
   overlapping read/planning layer; the mark/owner/scope API is net-new.

## Second pass — after the gcs↔cw-s3 de-novo factor (same day)

The peers ran a big "union / de-novo factor" (one codebase, per-store gates —
their monorepo direction; joint doc `specs/convergence.md`, four naming seams).
It landed almost entirely in the **mgu-only** surfaces (`site/`/`cloud/`/`job/`),
which are arch-divergent from dt's `ui/` — **not upstreamable**. Shared surfaces
stayed dt-ahead (433/4900).

It did surface **3 self-contained shared-package UX features** dt lacked, now
ported (commit `58f5b01`):

- `<TimeSeries>` slide-the-window drag (fixed point-span) + grab cursors.
- `<Treemap>` ⌥-click pins a branch instead of drilling.
- `<Treemap>` Backspace/Escape drill-pop guards `e.defaultPrevented`, on `window`.

## Third pass — gcs↔cw-s3 still diverged; one TimeSeries CP (2026-09-17)

Checked whether the two peers had *settled* their alignment (the monorepo union):
they have **not**. `m/gcs 4b65b18` ↔ `m/cw-s3 02906dc` still differ by **223
files, 8170 / 6011** — concentrated in the mgu-only deployment surfaces (`cloud`
3469/477, `site` 2609/536, `job` 652/623) + spec churn (1357/4207). cw-s3 has
moved *ahead* (`/api/series?split=roots` "root geneses"). Not upstreamable, and
the cloud-stack roadmap stays gated until the union lands.

The **only shared-surface delta between the two peers** is
`packages/react/src/TimeSeries.tsx` (`src/disk_tree` is byte-identical) — generic
charting cw-s3 grew for root geneses, now ported (adapted, not a clean CP: dt's
base carries the slide-window from `58f5b01`):

- `getY0?` — a series becomes a **band** from `getY0` up to `getY` (stacked areas:
  each series' `y0` = the running sum below it); tooltip shows band height, fill
  0.35, y-range still spans `getY`.
- `dashBeforeX?` — the line before a threshold x is dashed (a total drawn before
  every component existed), solid after; they share the cut point.
- per-series `area?` — overrides the chart-wide `area` either way.

3 new exact-geometry specs (`TimeSeries.test.tsx`, 94 green); tsc clean.

## Fourth pass — gcs answered the seams; seam 4 (comms) routed to us (2026-09-17)

`/read mgu gcs`: it decided the four convergence seams in `specs/gcs-toward-union.md`
§5 (`def9eee`, mgu-local) and routed the work:

- **Seams 1–2 (sweep engine + unified `actions` ledger)** — built on gcs/cw-s3, not
  here; gcs's `manifest`/`execute` executor + cw's persistent plan *object* on top,
  owner-slice as a configurable eligibility policy, pluggable undo (`versioning |
  soft-delete | none`, store-adapter declares its menu). These **unblock roadmap
  item 3**: once the unified ledger exists on both branches, DT upstreams *that*
  design. Still gated — do not start.
- **Seam 3 (D1 lineage)** — collapse-time, mechanical, no DT involvement.
- **Seam 4 (digest/comms)** — **routed to DT now**, "never gated on the union" =
  our comms roadmap item 1. Factor mgu's `discord_api` + `digest` into a generic
  `disk_tree` notify/digest **engine** + per-deployment **profile**. Spec opened:
  `specs/comms-notify.md`.

Roadmap item 2 (IaC) also confirmed ungated. Shared `packages/*` + `src/disk_tree`
US is complete (passes 1–3). So startable now, in priority order: **seam 4 comms**
(spec written), then IaC.

## Cursor

Assessed `m/gcs` @ `4b65b18` and `m/cw-s3` @ `02906dc`, read gcs session to
`117362` (seam decisions). Shared surfaces at parity/dt-ahead. Active DT work:
seam 4 comms (`specs/comms-notify.md`). Deferred: mark/sweep engine (item 3, gated
on seams 1–2 landing on both mgu branches), IaC (item 2, ungated, not started).
