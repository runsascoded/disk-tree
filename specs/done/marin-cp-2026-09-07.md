# CP manifest: marin `gcs` → upstream, 2026-09-07

Written by the marin-gcs-usage `gcs` session after `/cp dt/main` (cursor `dt/main 41978ca`). One shared-surface change since `marin-cp-2026-09-04.md`; everything else on `gcs` since `8191de0` is `site/`, `job/`, `gcs-usage/` (marin-only).

## `packages/react/src/TimeSeries.tsx` — `onBrush` + `window` (gcs `f30230c`)

Additive, default-off, same shape as the 9/4 props:

- `onBrush?: (x0, x1) => void` — drag across the plot picks an x-range, both ends snapped to data x's; the release is committed from a `window` `mouseup` listener (`{ once: true }`) so a drag that ends past the plot edge still lands, and a zero-width drag falls through to `onPickX`. With a brush, the SVG's `onClick` is dropped (the pick resolves in mouseup) so a click never fires twice. The in-progress drag lives in a ref (state flushes in a microtask under `createRoot`; down/move/up in quick succession would read stale closures) with a state copy for rendering.
- `window?: [number, number]` — a shaded band behind the series (`--dt-ts-window`, edges `--dt-ts-window-edge`, dashed); a drag in progress draws the same band solid (`--dt-ts-brush`). Cursor: `col-resize` while dragging, `crosshair` with a brush, `pointer` with only `onPickX`.
- `userSelect: 'none'` on the SVG, `preventDefault` on mousedown.

Take it from `m/gcs:packages/react/src/TimeSeries.tsx` (only the `@rdub/treemap` vs `./colors` import differs — gcs is still pre-extraction). Tests to add (gcs has none for these): drag → `onBrush(min, max)` with snapped ends; zero-width drag → `onPickX` once and `onBrush` never; `window` renders the band + two edge lines; no `onClick` pick when `onBrush` is set.

**Landed (DT, 2026-09-07):** applied as the exact per-file delta `git diff HEAD m/gcs -- packages/react/src/TimeSeries.tsx` (71+/4−; the import line already matched — gcs `e9a3cce` had taken the `@rdub/treemap` extraction). Tests added in `packages/react/tests/TimeSeries.test.tsx` at 400×200 (plot px 56/220/384 for t = 0/1/2): drag 2→1, release past the left edge → `onBrush(0, 2)` with a solid band and `col-resize` meanwhile, cleared after; zero-width drag → `onPickX(0)` once, `onBrush` never, and a subsequent hover+click doesn't re-fire; `window={[0, 1]}` → dashed band `x=56 w=164` with edges at 56/220, inert to presses without `onBrush`. 91 react-package tests pass.

## Not owed

- `gcs` `5b65b06` is this pass's CP *from* upstream (`3d48a20` tests, `3663e39` `find` fallback).
- `tree_build.py` docstring path (`marin/` → `gcs-usage/`) — marin's layout, not core.
